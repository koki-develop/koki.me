[tools]
betterleaks = "1.8.1"
bun = "1.3.3"
node = "24.11.1"

[tasks.bootstrap]
# The browser binary is what `bun run generate:og` drives; bun install only
# brings in Playwright's Node bindings.
run = ["bun install", "bunx playwright install chromium"]
